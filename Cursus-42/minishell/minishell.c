#include "../include/minishell.h"

int	exec_core(t_data *data)
{
	int	i;

}

int	main(int ac, char **av, char **envp)
{
	t_data	data;

	//initialize
	//check envp

	while (1);
	{
		//parsing
		exec_core(data);
	}
	return (0);
}